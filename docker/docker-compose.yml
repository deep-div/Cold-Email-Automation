version: "3.9"

services:
  app:
    container_name: cold-email-app-container 
    build:
      context: .. # Where docker will look into -> go one folder up from docker/ → Cold-Email-Automation/
      dockerfile: docker/Dockerfile
    image: deepdiv/cold-email-app:latest
    ports:
      - "8501:8501"
    environment:
      PYTHONPATH: /Cold-Email-Automation
